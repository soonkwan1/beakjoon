vector<pair<int,int> > ans;
        // vector<int> tmp;
        // dfs(0, nums, tmp, ans);
        // sort(ans.begin(), ans.end(), compMax());
        // cout << ans[0].first << " " << ans[0].second << endl;